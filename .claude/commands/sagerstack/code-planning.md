Invoke the `sagerstack-code-planning` skill.

This is the mandatory first step before any implementation. It produces `docs/project-context.md` with milestones, phases, and E2E test definitions through an interactive Q&A session.

User input: $ARGUMENTS
